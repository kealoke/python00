# ft_package

A playful, minimal package created for Exercise 09. It provides a simple utility 
to count the occurrences of an item in any list.

## Installation

You can install the package directly via pip:
```bash
pip install ft_package